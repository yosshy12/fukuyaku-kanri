# Apps Script設定手順

本人用と母用のスプレッドシートで、以下の操作をそれぞれ1回ずつ行います。

1. スプレッドシートを開き、`拡張機能` → `Apps Script`を選びます。
2. 最初から入っているコードをすべて削除し、このフォルダーの`apps-script/Code.gs`の内容を貼り付けます。
3. 上部の関数選択で`setup`を選び、`実行`を押します。
4. 初回の権限確認を許可します。
5. 実行ログに表示された`接続キー`を控えます。
6. `デプロイ` → `新しいデプロイ` → 種類`ウェブアプリ`を選びます。
7. `次のユーザーとして実行`は`自分`、アクセスできるユーザーは`全員`を選び、デプロイします。
8. 表示された`ウェブアプリURL`を控えます。
9. 服薬管理アプリ左上のメニューボタンを開き、URLと接続キーを入力します。

本人のスマホには本人用スプレッドシートで発行したURLとキーを設定し、母のスマホには母用で発行したURLとキーを設定します。アプリのURLは同じですが、保存先は完全に別になります。

## コードを更新した場合

現在は自動更新を設定済みです。`publish-github.bat`を実行すると、Apps Scriptに変更がある場合だけ、本人用・母用の両方へ同じコードを反映して既存のウェブアプリを再デプロイします。その後、GitHubへの送信とGitHub Pagesの公開が行われます。

接続キーとウェブアプリURLは変わりません。Apps Script画面でコードを貼り直したり、手動で再デプロイしたりする必要もありません。

Cloudflareは現在このアプリでは使用していません。公開先はGitHub Pagesであり、GitHubへの送信後に自動更新されます。
